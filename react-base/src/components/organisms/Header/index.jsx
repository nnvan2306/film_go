import { HEADER_HEIGHT } from "../../../constants";

const Header = () => {
    return (
        <div className={`bg-fieryRed`} style={{ height: `${HEADER_HEIGHT}px` }}>
            Header
        </div>
    );
};

export default Header;
