import { HEIGHT_BANNER } from "../../../constants";

const Banner = () => {
    return (
        <div style={{ height: `${HEIGHT_BANNER}px` }} className="bg-green-300">
            Banner
        </div>
    );
};

export default Banner;
