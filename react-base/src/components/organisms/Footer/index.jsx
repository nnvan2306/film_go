import { MIN_HEIGHT_FOOTER } from "../../../constants";

const Footer = () => {
    return (
        <div className="bg-dark" style={{ height: `${MIN_HEIGHT_FOOTER}px` }}>
            Footer
        </div>
    );
};

export default Footer;
