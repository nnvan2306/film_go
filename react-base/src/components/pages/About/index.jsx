import MainTemplate from "../../templates/MainTemplate";

const index = () => {
    return (
        <MainTemplate>
            <div>about</div>
        </MainTemplate>
    );
};

export default index;
